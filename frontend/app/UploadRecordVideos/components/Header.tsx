import React from 'react';
import styles from "../page.module.css";

const Header = () => {
    return (
        <div className={styles.titleSubtitleContainer}>
            <h2 className={styles.title}>Upload / Record videos </h2>
        </div>
    );
};

export default Header;